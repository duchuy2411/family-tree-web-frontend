import { makeStyles } from "@material-ui/core";

const useResetPasswordPageStyles = makeStyles(() => ({
  root: {
    minHeight: "70vh",
  },
  paperFormWrapper: {
    padding: 48,
  },
  marginTop12: {
    marginTop: 12,
  },
  marginBot24: {
    marginBottom: 24,
  },
  alert: {
    marginBottom: 8,
  },
  button: {
    marginTop: 12,
    height: 50,
  },
}));

export default useResetPasswordPageStyles;
